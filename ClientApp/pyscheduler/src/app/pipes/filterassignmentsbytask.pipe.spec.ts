import { FilterassignmentsbytaskPipe } from './filterassignmentsbytask.pipe';

describe('FilterassignmentsbytaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterassignmentsbytaskPipe();
    expect(pipe).toBeTruthy();
  });
});
