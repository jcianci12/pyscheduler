import { FiltereventsbydatePipe } from './filtereventsbydate.pipe';

describe('FiltereventsbydatePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltereventsbydatePipe();
    expect(pipe).toBeTruthy();
  });
});
