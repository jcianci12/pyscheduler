import { PersonbookedthiseventPipe } from './personbookedthisevent.pipe';

describe('PersonbookedthiseventPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonbookedthiseventPipe();
    expect(pipe).toBeTruthy();
  });
});
