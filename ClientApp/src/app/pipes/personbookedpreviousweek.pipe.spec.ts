import { PersonbookedpreviousweekPipe } from './personbookedpreviousweek.pipe';

describe('PersonbookedpreviousweekPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonbookedpreviousweekPipe();
    expect(pipe).toBeTruthy();
  });
});
