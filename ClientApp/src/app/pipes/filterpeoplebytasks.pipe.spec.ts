import { FilterpeoplebytasksPipe } from './filterpeoplebytasks.pipe';

describe('FilterpeoplebytasksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpeoplebytasksPipe();
    expect(pipe).toBeTruthy();
  });
});
