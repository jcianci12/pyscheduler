import { FilterpeoplebynamePipe } from './filterpeoplebyname.pipe';

describe('FilterpeoplebynamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpeoplebynamePipe();
    expect(pipe).toBeTruthy();
  });
});
