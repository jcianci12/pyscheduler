import { DuplicatepersonPipe } from './duplicateperson.pipe';

describe('DuplicatepersonPipe', () => {
  it('create an instance', () => {
    const pipe = new DuplicatepersonPipe();
    expect(pipe).toBeTruthy();
  });
});
