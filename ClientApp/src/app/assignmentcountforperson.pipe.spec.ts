import { AssignmentcountforpersonPipe } from './assignmentcountforperson.pipe';

describe('AssignmentcountforpersonPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignmentcountforpersonPipe();
    expect(pipe).toBeTruthy();
  });
});
